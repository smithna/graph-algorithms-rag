reveal.js 5.2.1 — https://github.com/hakimel/reveal.js — MIT (see LICENSE).
Vendored deliberately: the deck must present from a local file with no network.
Only the UMD builds are copied; the .esm.js variants are omitted because ES
modules are blocked by CORS under file://.
